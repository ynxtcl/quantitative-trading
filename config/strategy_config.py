"""
========================================
  三策略参数配置 — 各自独立
========================================

【参数配置原则】
1. 每个策略的参数完全独立，互不影响
2. 参数值背后的逻辑：基于实盘经验和金融学原理
3. 注释说明每个参数的"为什么是这个值"、"调参方向"和"风险"
4. 只有核心参数放在这里，次要参数直接写在策略代码里

【2026-07-01 优化（夏普提升 + 回撤降低）】
核心调整：
  - TF: ADX threshold 20→25，退出通道 15→20（减少假突破，让趋势多奔跑）
  - MR: trend_filter 0.97→0.92，position_weight 0.5→0.7（放宽入场，真正发挥对冲作用）
  - FS: top_n 3→5（10只股票选5只，分散更充分）
  - 组合权重：40/20/40 → 35/25/40（MR提升权重匹配其预期对冲作用）
"""

# ==================== 策略A：趋势跟踪 ====================
TREND_FOLLOWING_CONFIG = {
    "name": "trend_following",
    "weight": 0.35,                 # 组合权重 35%（2026-07-01优化：自40%降至35%）
    # 权重下调原因：ADX阈值提高后信号减少，但仍为核心盈利引擎
    # 降低5%给MR，增强震荡市对冲效果
    # 数据源：evaluate_strategies.py 独立回测

    # ★ 通道突破参数 ★
    "entry_period": 20,             # 唐奇安通道突破周期 = 20日
    # 为什么是20？约等于1个自然月（22个交易日），是经典唐奇安通道参数
    # 20日突破表示"创过去一个月新高"——突破的含金量高，过滤假突破
    # 调参方向：增大→减少交易次数，提高胜率但可能错过行情
    #           减小→更多交易，捕捉更多趋势但假突破增多
    "exit_period": 20,              # 止损通道周期 = 20日（2026-07-01优化：自15日升至20日）
    # 出场周期从15→20：进一步放宽出场条件，让趋势有更大空间奔跑
    # 20日低点 ≈ 过去一个月的最低点，跌破意味着中期趋势可能反转
    # 配合组合级 trailing_stop=15% 和固定止损20%形成三层保护

    # ★ 过滤条件 ★
    "ma_filter_period": 60,         # 趋势过滤均线 = 60日（约3个月）
    # 60日均线（季线）是A股最重要的趋势线之一
    # 只在价格高于60日线做多 → 避免在下降趋势中逆势买入
    # 这是最简单的"顺势而为"表达
    "adx_threshold": 25,            # ADX过滤阈值（2026-07-01优化：自20升至25）
    # ADX（平均趋向指数）衡量趋势强度的指标
    # 0-20：弱趋势（震荡市）  20-40：中等趋势  40+：强趋势
    # 原20 → 新25：提高入场门槛，排除ADX=20~25的弱趋势交易
    # 目的是减少在"看似有趋势实则震荡"的市场中频繁进出
    # 预期：信号数量减少，但胜率和盈亏比提升

    # 仓位
    "position_weight": 1.0,         # 单次信号仓位 = 100%
    # 趋势跟踪策略的仓位是满仓的（1.0）
    # 因为趋势信号本身就包含了对方向的强烈判断
    # 注意：这是信号权重，实际仓位还会被引擎的可用资金限制

    # ★ EMA20 辅助过滤 (2026-07-01 Step B) ★
    "ema_filter_enabled": True,        # 启用 EMA20 辅助过滤（Step B）
    "ema_filter_period": 20,           # EMA20 周期
    # 问题：MA60 反应太慢（~3个月均线），2022年下跌中逆势开仓
    # 方案：当 close < EMA20 时，即使 ADX>25 也不开新仓
    # EMA20 反应快，可在下跌初期阻止逆势开仓
    # 预期：回撤 -3~5pp，信号数量 ~-15%，夏普基本不变

    # ★ ATR 动态仓位控制 (2026-07-01) ★
    "use_atr_sizing": True,         # 是否启用 ATR 动态仓位
    "atr_sizing_threshold": 12,     # ATR比例阈值：close/atr < 12 时缩减仓位
    # 当 close/atr < 12 表示当前波动率极高（如股价10元，ATR>0.83）
    # 此时仓位按比例缩减，避免高波动时重仓被震出场
    # 逻辑：close/atr = 股价 ÷ 日均波动幅度
    #       比值越小 = 相对波动越大 = 需要降低仓位

    # ★ 分阶段止盈 (2026-07-01 Step A) ★
    "take_profit_enabled": True,       # 启用分阶段止盈（2026-07-01 Step A）


    "take_profit_lookback": 10,        # 10日高点参考
    "take_profit_drawdown": 0.08,      # 从高点回落8%触发
    "take_profit_exit_ratio": 0.5,     # 平掉一半仓位
    # 问题：TF 只有通道跌破出场(exit_period=20)，没有主动止盈
    #   2022年单边下跌中，TF 持仓从浮盈→浮亏，坐了完整过山车
    # 方案：当价格从最近10日高点回落>8%时，平掉一半仓位锁定利润
    #   剩余仓位继续按原规则奔跑，不错过后面的趋势
    # 预期：夏普+0.05~0.08，回撤-3~5pp

}

# ==================== 策略B：均值回归 ====================
MEAN_REVERSION_CONFIG = {
    "name": "mean_reversion",
    "weight": 0.25,                 # 组合权重 25%（2026-07-01优化：自20%升至25%）
    # 独立年化 -0.83%，但在组合中起核心对冲作用
    # 权重上调原因：
    #   1. 放宽参数后MR信号会增多，能更好地在震荡市中对冲TF亏损
    #   2. TF权重下调5% + FS维持40% = MR升至25%
    # 数据源：evaluate_strategies.py 独立回测 + 2026-07-01优化

    # ★ 布林带参数 ★
    "bb_period": 20,                # 布林带周期 = 20日
    "bb_std": 2.0,                  # 标准差倍数 = 2
    # 统计学依据：正态分布下，2σ包含约95%的价格数据

    # ★ RSI参数 ★
    "rsi_period": 14,               # RSI默认周期
    "rsi_oversold": 30,             # 超卖阈值 = 30
    "rsi_overbought": 70,           # 超买阈值 = 70
    # 经典设定：<30 超卖（适合买入），>70 超买（适合卖出）

    # ★ 趋势过滤 ★ (v2 调整: 0.85→0.97→0.92 平衡接飞刀风险与入场机会)
    "trend_filter": True,
    "trend_filter_level": 0.92,     # 2026-07-01优化：自0.97降至0.92
    # v1: 0.85 — 允许低于趋势线15%，接飞刀风险极高
    # v2: 0.97 — 要求接近趋势线，大幅减少逆势交易，但信号过少(仅10.6%)
    # v3: 0.92 — 折中方案，允许价格在EMA50下方8%范围内入场
    #   0.92 ≈ 在中期趋势略微向下的情况下也允许做均值回归
    #   预期：MR信号从10.6%提升至约18-22%，更好地发挥对冲作用

    # ★ 布林带宽度过滤 ★
    "min_bb_width": 0.05,           # 最小宽度 5%（防压缩入场）
    "max_bb_width": 0.20,           # 最大宽度 20%（防极端波动）

    # ★ 成交量确认 (v2 新增) ★
    "volume_filter": True,          # 是否启用成交量确认
    "vol_low_threshold": 0.6,       # 缩量阈值：<20日均量的60%（2026-07-01优化：自0.8放宽至0.6）
    # 原0.8：要求缩量<80%，排除不够"缩"的情况
    # 新0.6：要求缩量<60%，更精准地捕捉"抛压衰竭"信号
    # 同时保持 vol_high_threshold=2.0 作为放量恐慌入场的渠道
    "vol_high_threshold": 2.0,      # 放量阈值：>20日均量的200%
    # 买点需要缩量止跌(抛压衰竭)或恐慌性放量(底部换手)
    # 排除横盘无量下跌的"阴跌"情况（均值回归陷阱）

    # ★ ATR 动态止损 (v2 新增) ★
    "atr_period": 14,               # ATR计算周期
    "stop_loss_atr": 1.5,           # 止损ATR倍数
    # 止损线 = bb_lower - stop_loss_atr × ATR
    # 例如：bb_lower=10, ATR=0.5, 倍数=1.5
    #       → 止损=10-1.5*0.5=9.25（跌破7.5%触发）
    # ATR 会自动适应市场波动：
    #   高波动时(ATR大)止损宽 → 避免被噪音止损
    #   低波动时(ATR小)止损窄 → 严格控制风险

    # ★ 偏离度置信度增强 (2026-07-01 新增) ★
    "use_deviation_confidence": True,  # 是否启用偏离度置信度
    # 偏离度 = (close - bb_ma) / bb_std ≈ Z-score
    # 偏离度绝对值越大 → 回归信号越强 → 置信度越高
    # 原实现使用固定 confidence，现改为动态：
    #   Z=2 → confidence=0.67, Z=3 → confidence=1.0
    # 这样偏离越远的信号在组合器中的话语权越大

    # 仓位
    "position_weight": 0.7,         # 均值回归仓位 = 70%（2026-07-01优化：自0.5升至0.7）
    # 原0.5：过于保守，即使入场也只敢用一半资金
    # 新0.7：更充分地利用回归信号，同时保留30%的缓冲
    # 预期：MR单笔盈利贡献提升

    # ★ 市场状态动态仓位 (2026-07-01 调整 C) ★
    "market_adaptive_weight": False,    # 是否启用市场状态自适应（默认关闭，保持旧行为）
    # 开通后 MR 买入信号会根据 market_median_return 自动缩仓：
    #   普跌(mmr<-3%) → 缩至 weak_market_weight
    #   严重(mmr<-8%) → 缩至 severe_market_weight
    # 原理：市场普跌时 MR 做多 = 接飞刀
    # 当前默认 False，确认效果后再永久启用
    "weak_market_threshold": -0.03,     # 弱势市场阈值：全市场20日中位数回报 < -3%
    "severe_market_threshold": -0.08,   # 严重弱势阈值：全市场20日中位数回报 < -8%
    "weak_market_weight": 0.3,          # 弱势市场信号仓位保留 30%
    "severe_market_weight": 0.15,       # 严重弱势市场信号仓位保留 15%
}

# ==================== 策略C：因子选股 ====================
FACTOR_SELECTION_CONFIG = {
    "name": "factor_selection",
    "weight": 0.40,                 # 组合权重 40%
    # 独立年化 +7.95%，保持40%不变
    # 因子选股是选股维度（与择时不同），组合对冲效果好
    # 数据源：evaluate_strategies.py 独立回测

    # 调仓频率
    "rebalance_freq": "monthly",    # 每月调仓
    # 为什么按月而不是按天？
    # 1. 因子通常有3-6个月的半衰期，月度调仓足够捕获因子收益
    # 2. 过度频繁调仓会增加交易成本（佣金+滑点）
    # 3. 基本面因子（PE/ROE）本身变化缓慢
    "top_n": 5,                     # 选前N只（2026-07-01优化：自3升至5）
    # 原3只：在10只股票中选3只，过于集中（000333曾达50.6%）
    # 新5只：选一半的股票，分散更充分
    # 预期效果：降低单票集中度风险，持股更均衡
    # 潜在代价：可能纳入得分较低的股票，略微降低选股纯度

    # ★ 多因子定义 ★
    # 每只因子包含：名称、权重、方向
    # direction: 1=越大越好，-1=越小越好
    "factors": [
        {"name": "pe", "weight": 0.20, "direction": -1},
        # PE（市盈率）：越低越好（价值投资逻辑）
        # 但要注意：低PE可能是"价值陷阱"（公司基本面恶化的假象）
        # 所以权重只给20%，不单独依赖

        {"name": "roe", "weight": 0.30, "direction": 1},
        # ROE（净资产收益率）：越高越好（盈利能力）
        # ROE ≥ 15%是大牛股的典型特征（茅台常年30%+）
        # 给最高权重30%（Step D: 自25%提升），因为ROE是预测未来收益最稳定的单一因子

        {"name": "momentum_1m", "weight": 0.10, "direction": 1},
        # 动量因子：近1月涨幅越高越好
        # Step D: 权重自0.20降至0.10
        # 原因是动量是顺周期因子，熊市中追跌，回撤贡献大
        # 动量效应的心理学基础：
        #   1. 锚定效应 — 投资者对新信息反应不足
        #   2. 羊群效应 — 趋势形成后自我强化
        # 但动量因子在A股反转时可能剧烈回撤

        {"name": "volume_ratio", "weight": 0.15, "direction": 1},
        # 量比因子：成交量/均量 > 1 = 放量
        # 价量关系：放量上涨 = 资金流入，缩量上涨 = 动能不足
        # 权重最低15%，因为量比不稳定，单日异常值多

        {"name": "volatility", "weight": 0.25, "direction": -1},
        # 波动率因子：越低越好（低波动异象）
        # Step D: 权重自0.20升至0.25，强化熊市防御
        # 学术研究发现：低波动股票长期回报反而高于高波动股票
        # 这就是著名的"低波动异象"（Low Volatility Anomaly）
        # 可能的解释：高波动股票被散户追捧（彩票偏好），估值过高
    ],

    "score_method": "equal_weight",     # 等权打分
}


# ==================== 策略D：XGBoost ML 信号 (Phase 4.5) ====================
XGBOOST_CONFIG = {
    "name": "xgboost",
    "weight": 0.30,                 # 组合权重 30%
    # Phase 4.5 新增 — ML 信号辅助
    # 权重30%：XGBoost 需要先用 WF 验证取得稳定的 OOS Sharpe
    # 建议在 main_phase4.py 独立验证后根据 XGBoost 胜率调整
    # 保守起步30%，逐步上调

    # ★ ML 信号阈值 ★
    "threshold_buy": 0.55,          # 买入阈值（默认 0.55）
    "threshold_sell": 0.45,         # 卖出阈值（默认 0.45）
    # P(上涨) > 0.55 → 买入（需要一定置信度）
    # P(上涨) < 0.45 → 卖出（看跌信号）
    # 0.45~0.55 → 持有（不确定区域，过滤噪音）

    "position_weight": 1.0,         # 单次信号仓位 = 100%
    # ML 信号本身已反映置信度，所以信号权重给满
    # 实际仓位会被 combiner 的权重(30%)和风控系统限制

    # ★ 特征列引用 ★
    "feature_cols": None,           # None → 使用 FEATURE_COLS 默认22维
    # 如果只想用特征子集，可以传入自定义列名列表
    # 例如：["return_1d", "return_5d", "ma_60_ratio", "volatility_20"]
}
