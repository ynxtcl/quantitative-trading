"""
========================================
 定量交易系统 — 全局配置
========================================

【配置层的设计哲学】
配置集中管理是量化系统的第一原则。
- 所有可调参数集中在 config/ 下，不散落在代码中
- 回测参数、数据参数、策略参数分离，各自独立修改
- 路径自动化，不依赖硬编码绝对路径
"""

import os
from pathlib import Path

# ==================== 路径配置 ====================
# Path(__file__).parent.parent 两次 parent 从 settings.py 回到项目根目录
# __file__ 是脚本的完整路径，dirname 取目录名
# 最终效果：无论从哪个目录执行 main.py，路径都是正确的
PROJECT_ROOT = Path(os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
DATA_DIR = PROJECT_ROOT / "data_storage"
DATA_DIR.mkdir(exist_ok=True)  # exist_ok=True 表示目录已存在时不报错

# ==================== 回测参数 ====================
BACKTEST_CONFIG = {
    "initial_capital": 100000.0,       # 初始资金 — 10万元（初学者实战起点）
    "commission": 0.0003,               # 佣金比例 (万三)
    "min_commission": 5.0,              # A股最低佣金 5元（max(5, 成交额×万三)）
    # 为什么是万三？A股散户实际佣金在万1.5~万3之间，取上限更保守
    # min_commission=5 模拟了 A 股"最低 5 元"的行业惯例
    # 对小账户（<50万）影响显著，对大账户影响小
    "stamp_tax": 0.001,                 # 印花税 (千一, 卖出时收取)
    # 注意：只在卖出时收取，买入不收。这是A股的独特规则
    # 2023年8月已从千一降到万五，这里用千一是保守估计
    "slippage": 0.001,                  # 滑点比例 (千一)
    # 滑点 = 理论成交价与实际成交价的差异
    # 对大盘股（沪深300成分股）千一足够，对小盘股可能需要千三以上
    # 滑点的本质：你的订单对市场价格的冲击 + 买卖价差
    "freq": "1d",                       # 回测频率: 1d=日频
    # 目前仅支持日频，因 akshare 免费数据仅提供日K
    # 如需分钟级回测需要切换数据源（如 tushare pro / wind）
    "start_date": "2020-01-01",
    "end_date": "2025-01-01",
    # 5年跨度：覆盖了2020疫情暴跌、2021核心资产牛市、2022~2024震荡熊市
    # 包含完整牛熊周期，策略测试更有说服力
}

# ==================== 交易标的 ====================
# 扩充至10只（覆盖8大行业，组合多样性更好）
# 2026-07-01 优化：自5只扩展至10只
# 新增：招商银行(银行)、贵州茅台(白酒)、中国平安(保险)、
#       万华化学(化工)、科大讯飞(人工智能)
DEFAULT_SYMBOLS = [
    "000001",   # 平安银行 — 银行蓝筹
    "000333",   # 美的集团 — 家电龙头
    "000858",   # 五粮液   — 白酒
    "002415",   # 海康威视 — 安防科技
    "300750",   # 宁德时代 — 新能源
    "600036",   # 招商银行 — 银行
    "600519",   # 贵州茅台 — 白酒
    "601318",   # 中国平安 — 保险
    "600309",   # 万华化学 — 化工
    "002230",   # 科大讯飞 — AI科技
]

# ==================== 数据参数 ====================
DATA_CONFIG = {
    "cache_enabled": True,              # 启用本地缓存
    # 为什么必须缓存？akshare 每次调用是网络请求，可能有500ms~3s延迟
    # 5只股票5年数据一次性下载仅需5-15秒，但如果每次都重新下载
    # 调试策略时需要反复运行，时间浪费在等待网络而不是分析结果
    "cache_format": "parquet",          # 缓存格式: parquet / csv
    # parquet 比 csv 优势：
    #   1. 列式存储，读取速度快3-10倍
    #   2. 自动保留数据类型（日期、浮点数精度）
    #   3. 压缩比高（同数据 parquet 约 csv 的1/3大小）
    #   4. 支持按列读取（未来数据量大时只需要读 close 列）
    "max_history_years": 5,             # 最多下载5年数据
    # 5年是 akshare 免费数据的稳定区间
    # 超过5年可能返回空或错误数据
    "freq_check": True,                 # 频率校验，拒绝非日频数据
}

# ==================== 行业分类 ====================
# 用于 C2 行业集中度风控（限制单行业总暴露）
# 2026-07-01 更新：同步扩充至10只股票
INDUSTRY_MAP = {
    "000001": "银行",
    "000333": "家电",
    "000858": "白酒",
    "002415": "科技",
    "300750": "新能源",
    "600036": "银行",
    "600519": "白酒",
    "601318": "保险",
    "600309": "化工",
    "002230": "科技",
}


# ==================== 风控参数 ====================
# 2026-07-01 v2 更新：回撤优化（3层防护，前置风控）
# 核心调整：
#   1. 硬熔断 50%→30%，新增 dd_recovery_days=10 回撤恢复期（防止刚恢复又加仓被套）
#   2. 新增 market_weakness 广谱下跌过滤器：过半股票20日跌>5%时自动降仓50%
#   3. 保留现有止损（20%）和 trailing_stop（15%）不动——诊断证明收紧对回撤无效
#   4. 保留 progressive_drawdown 和 vol_target（加码stage阈值前移）
#   5. stop_loss / max_single_weight / consensus_multiplier 均不变——保护年化收益

# 覆盖 RiskManager 默认配置
# 传入 None 则使用 RiskManager 内部默认值
RISK_CONFIG = {
    "max_single_weight": 0.50,           # 单标的仓位上限 50%（不变 — 收紧会削弱MR信号执行）
    "max_total_position": 0.95,          # 总仓位上限 95%
    "stop_loss": 0.20,                   # 组合级止损 20%（不变 — 收紧对回撤无效，只会增误触发）
    "max_daily_loss": 0.05,              # 每日最大亏损 5%
    "max_daily_symbols": 5,              # 每日最多交易 5 只不同股票
    "max_drawdown": 0.30,                # ★ 组合最大回撤熔断 30%（自50%降至30% — 更早触发保护）
    "enforce_stop_loss": True,           # 是否在引擎层面强制止损
    "trailing_stop": 0.15,              # 移动止损：从持仓期间最高价回落15%触发（不变）
    # C1: 波动率自适应仓位 — 已关闭（诊断表明破坏收益>保护效果）
    "vol_adaptive": False,               # 已禁用（自diagnose_risk_impact.py诊断结果）
    "vol_low": 0.15,                     # 低波动基准（年化15%，满仓）
    "vol_high": 0.40,                    # 高波动基准（年化40%，仓位减半）
    # C2: 行业集中度
    "max_industry_weight": 0.50,          # 单行业总暴露上限 50%
    "industry_map": INDUSTRY_MAP,         # 股票代码 → 行业映射
    # ★ 2026-07-01 新增：渐进式回撤缩仓（替代硬熔断）
    "progressive_drawdown": True,         # 是否启用渐进式回撤缩仓
    "dd_stage1": 0.15,                   # 回撤15%以上：仓位上限降至70%
    "dd_stage2": 0.25,                   # 回撤25%以上：仓位上限降至50%
    "dd_stage3": 0.35,                   # 回撤35%以上：仓位上限降至30%
    # ★ 2026-07-01 v2 新增：回撤恢复期（冷却机制）
    "dd_recovery_days": 10,              # 熔断触发后需连续N天净值回升才能重新开仓
    # ★ 2026-07-01 新增：波动率目标参数
    "vol_target": 0.18,                   # 目标年化波动率 18%
    "vol_target_lookback": 20,            # 波动率计算窗口（20个交易日）
    # ★ 2026-07-01 v2 新增：广谱下跌过滤器（前置风控）
    "market_weakness_enabled": True,      # 是否启用广谱下跌过滤
    "market_weakness_threshold": -0.05,   # 过半股票20日跌超5% → 判定为弱势市场
    "market_weakness_severe": -0.10,      # 严重弱势：过半股票20日跌超10%
    "market_weakness_lookback": 20,       # 广谱下跌计算窗口
    "market_weakness_pos_ratio": 0.50,    # 触发阈值：50%以上股票呈弱势
}


# ==================== 实盘 / Mock 交易配置 ====================
TRADING_CONFIG = {
    "mode": "mock",                      # "mock" | "live"
    "broker": "mock",                    # 券商类型: "mock" | "xtquant"
    "commission": 0.0003,                # 佣金比例 (万三)
    "min_commission": 5.0,               # A股最低佣金 5元
    "stamp_tax": 0.001,                  # 印花税 (千一, 卖出时收取)
    "slippage": 0.001,                   # 滑点比例 (千一)
    "lot_size": 100,                     # A股最小交易单位 (1手=100股)
    "check_interval_seconds": 300,       # 盘中检查间隔（5分钟）
    "order_timeout_seconds": 30,         # 订单超时（30秒）
    "max_retries": 3,                    # 下单失败重试次数
    "fill_probability": 0.95,            # Mock成交概率 (大盘股)
    "partial_fill_prob": 0.30,           # Mock部分成交概率
    "min_delay": 0.5,                    # Mock最小延迟(秒)
    "max_delay": 3.0,                    # Mock最大延迟(秒)
    "max_daily_symbols": 5,              # 每日最多交易标的数
    "account_id": "",                    # 资金账号 (实盘用)
}

# ==================== 股票池筛选参数 (Phase 2.5) ====================
# 当 akshare 网络失败时的回退候选池
# 覆盖沪深300主要行业：金融/家电/白酒/新能源/银行/科技/面板/保险/医药
# 消费/白酒/养殖/地产/家电/化工/券商/银行/银行/券商
# 光伏/建材/有色/传感器/连接器
SCREENER_FALLBACK_SYMBOLS = [
    "000001", "000333", "000858", "600519", "300750",  # 金融/家电/白酒/新能源
    "600036", "002415", "000725", "601318", "600276",  # 银行/科技/面板/保险/医药
    "600887", "002304", "002714", "000002", "600690",  # 消费/白酒/养殖/地产/家电
    "600309", "300059", "601166", "002142", "600030",  # 化工/券商/银行/银行/券商
    "601012", "600585", "601899", "300124", "002475",  # 光伏/建材/有色/传感器/连接器
]

SCREENER_CONFIG = {
    "index_name": "沪深300",
    "pool_size": 30,                       # 股票池目标数量
    "min_avg_amount": 50_000_000,          # 日均成交额 > 5000万
    "min_volatility": 0.02,                # 20日波动率 > 2%
    "max_pe": 60,                          # PE < 60
    "max_pool_turnover": 0.10,             # 股票池换手率上限 ≤ 10%
    "pool_persistence_file": str(DATA_DIR / "current_pool.json"),
    "pool_refresh_frequency": "quarterly", # 季度刷新
}


