"""
========================================
  trading/ — Mock 实盘交易系统（V2 事件驱动）
========================================

【定位】
将回测原型升级为事件驱动的模拟实盘系统。

【V2 改进 — 2026-06-29】
1. BrokerBase 新增 P0 接口:
   - get_order() / get_orders() — 查询委托
   - get_fills() — 查询成交
   - subscribe_quotes() — 实时行情订阅
   - on_fill() / on_disconnect() — 事件回调注册
   - reconnect() / get_multiple_prices() — 断线重连/批量行情
2. MockBroker V2:
   - 线程级异步成交处理 (不再阻塞主循环)
   - 成交后通过 on_fill callback 通知引擎
   - subscribe_quotes() 模拟盘中分时推送
   - get_orders() / get_fills() 完整接口
3. LiveEngine V2:
   - 事件驱动架构 (行情推送→策略推理→下单)
   - 注册成交回调 on_fill() 实时更新
   - on_error() 异常回调隔离
   - 支持 intraday 盘中多次推理架构

【mock vs 回测 核心区别】
┌──────────────┬─────────────────────────┬──────────────────────────┐
│     维度     │     回测 (Backtest)     │  模拟实盘 V2 (Mock)       │
├──────────────┼─────────────────────────┼──────────────────────────┤
│ 时间推进     │ 自动逐日循环             │ 事件驱动 (行情推送→推理)  │
│ 成交模型     │ 100%按收盘价成交          │ 概率成交 + 线程异步回调   │
│ 订单管理     │ 无（直接成交）            │ 完整生命周期 + 状态机     │
│ 滑点模型     │ 固定比例                  │ 动态（按流动性分层）      │
│ 延迟模拟     │ 无                      │ 随机延迟 + 后台线程       │
│ 风控时机     │ 信号产生后立即过滤        │ 下单前/成交后两次风控     │
│ 盘中频率     │ 每日1次推理              │ 架构支持每 tick 推理      │
│ 行情源       │ 日 K 线                  │ 当前日 K + 分时推送模拟   │
└──────────────┴─────────────────────────┴──────────────────────────┘

【目录结构】
trading/
├── __init__.py         模块声明（V2 文档）
├── order.py            订单/持仓/成交/账户 数据类（7个 dataclass）
├── broker_base.py      券商抽象接口（14个方法，P0/P1 分层）
├── mock_broker.py      模拟券商 V2（线程异步成交 + 完整查询接口）
├── order_manager.py    订单管理器（信号→订单转化 + 超时监控）
├── real_time_data.py   实时数据回放器（日 K 回放）
└── live_engine.py      实盘引擎 V2（事件驱动架构）
"""
